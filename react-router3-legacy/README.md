# react-router3-legacy

Legacy React Router v3 sample built with webpack, used to cover route config objects, `IndexRoute`, `getChildRoutes`, `onEnter`, and `browserHistory` navigation patterns.

## Commands

```bash
npm install
npm run start
npm run build
```
