import { sectionMap } from '../../config/sections'
import SectionPage from '../shared/SectionPage'

function SettingsPage() {
  return <SectionPage section={sectionMap.settings} />
}

export default SettingsPage
