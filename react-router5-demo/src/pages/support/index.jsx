import { sectionMap } from '../../config/sections'
import SectionPage from '../shared/SectionPage'

function SupportPage() {
  return <SectionPage section={sectionMap.support} />
}

export default SupportPage
