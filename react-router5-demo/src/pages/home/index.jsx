import { sectionMap } from '../../config/sections'
import SectionPage from '../shared/SectionPage'

function HomePage() {
  return <SectionPage section={sectionMap.home} />
}

export default HomePage
