import { sectionMap } from '../../config/sections'
import SectionPage from '../shared/SectionPage'

function ProfilePage() {
  return <SectionPage section={sectionMap.profile} />
}

export default ProfilePage
