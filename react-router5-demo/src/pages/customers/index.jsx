import { sectionMap } from '../../config/sections'
import SectionPage from '../shared/SectionPage'

function CustomersPage() {
  return <SectionPage section={sectionMap.customers} />
}

export default CustomersPage
