import { sectionMap } from '../../config/sections'
import SectionPage from '../shared/SectionPage'

function OrdersPage() {
  return <SectionPage section={sectionMap.orders} />
}

export default OrdersPage
