import { sectionMap } from '../../config/sections'
import SectionPage from '../shared/SectionPage'

function InventoryPage() {
  return <SectionPage section={sectionMap.inventory} />
}

export default InventoryPage
